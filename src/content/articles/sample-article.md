---
title: "Sample Article (draft — never published)"
description: "Keeps the articles pipeline exercised; delete when the first real article ships."
date: 2026-07-03
draft: true
---

This draft exists so the articles machinery (schema → index → article page) always has one file
to validate against. It is excluded from the index and from route generation by `draft: true`.

## What an article supports

Regular Markdown: **bold**, *italics*, `inline code`, [links](/projects/), lists, quotes, and
code blocks:

```python
def kelly_fraction(edge: float, odds: float) -> float:
    return edge / odds
```

> Blockquotes for pulled findings or caveats.

Delete this file when the first real article ships.
